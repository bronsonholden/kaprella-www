import { MobileResizeableDirective } from './mobile-resizeable.directive';

describe('MobileResizeableDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileResizeableDirective();
    expect(directive).toBeTruthy();
  });
});
