import { TouchResizeableDirective } from './touch-resizeable.directive';

describe('TouchResizeableDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchResizeableDirective();
    expect(directive).toBeTruthy();
  });
});
